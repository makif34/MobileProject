<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp8.Home"
             Title="Home">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Centered Text -->
        <StackLayout Grid.Row="0" VerticalOptions="CenterAndExpand" HorizontalOptions="Center">
            <Label Text="Simply Sales" FontSize="30" />
        </StackLayout>

        <!-- Bottom Text with Green Background -->
        <StackLayout Grid.Row="1" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" BackgroundColor="Green">
            <Label Text="MehmetAkifsanlan_200201050" FontSize="30" HorizontalOptions="Center" VerticalOptions="Center" />
        </StackLayout>
    </Grid>
</ContentPage>